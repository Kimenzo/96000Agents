import { Agent } from '@mastra/core/agent';

export const agent964 = new Agent({
  id: 'agent-964',
  name: 'Agent 964',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
