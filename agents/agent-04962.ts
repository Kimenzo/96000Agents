import { Agent } from '@mastra/core/agent';

export const agent4962 = new Agent({
  id: 'agent-4962',
  name: 'Agent 4962',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
