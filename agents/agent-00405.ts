import { Agent } from '@mastra/core/agent';

export const agent405 = new Agent({
  id: 'agent-405',
  name: 'Agent 405',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
