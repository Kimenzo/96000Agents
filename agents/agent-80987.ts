import { Agent } from '@mastra/core/agent';

export const agent80987 = new Agent({
  id: 'agent-80987',
  name: 'Agent 80987',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
