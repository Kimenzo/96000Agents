import { Agent } from '@mastra/core/agent';

export const agent983 = new Agent({
  id: 'agent-983',
  name: 'Agent 983',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
