import { Agent } from '@mastra/core/agent';

export const agent40406 = new Agent({
  id: 'agent-40406',
  name: 'Agent 40406',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
