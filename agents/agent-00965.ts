import { Agent } from '@mastra/core/agent';

export const agent965 = new Agent({
  id: 'agent-965',
  name: 'Agent 965',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
