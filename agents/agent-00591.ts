import { Agent } from '@mastra/core/agent';

export const agent591 = new Agent({
  id: 'agent-591',
  name: 'Agent 591',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
