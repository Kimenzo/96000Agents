import { Agent } from '@mastra/core/agent';

export const agent90886 = new Agent({
  id: 'agent-90886',
  name: 'Agent 90886',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
