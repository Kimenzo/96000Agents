import { Agent } from '@mastra/core/agent';

export const agent90883 = new Agent({
  id: 'agent-90883',
  name: 'Agent 90883',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
