import { Agent } from '@mastra/core/agent';

export const agent86256 = new Agent({
  id: 'agent-86256',
  name: 'Agent 86256',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
