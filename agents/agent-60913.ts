import { Agent } from '@mastra/core/agent';

export const agent60913 = new Agent({
  id: 'agent-60913',
  name: 'Agent 60913',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
