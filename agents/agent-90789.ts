import { Agent } from '@mastra/core/agent';

export const agent90789 = new Agent({
  id: 'agent-90789',
  name: 'Agent 90789',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
