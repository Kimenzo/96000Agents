import { Agent } from '@mastra/core/agent';

export const agent86306 = new Agent({
  id: 'agent-86306',
  name: 'Agent 86306',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
