import { Agent } from '@mastra/core/agent';

export const agent908 = new Agent({
  id: 'agent-908',
  name: 'Agent 908',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
