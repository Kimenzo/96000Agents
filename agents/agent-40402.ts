import { Agent } from '@mastra/core/agent';

export const agent40402 = new Agent({
  id: 'agent-40402',
  name: 'Agent 40402',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
