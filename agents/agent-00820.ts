import { Agent } from '@mastra/core/agent';

export const agent820 = new Agent({
  id: 'agent-820',
  name: 'Agent 820',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
