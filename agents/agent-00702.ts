import { Agent } from '@mastra/core/agent';

export const agent702 = new Agent({
  id: 'agent-702',
  name: 'Agent 702',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
