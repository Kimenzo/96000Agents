import { Agent } from '@mastra/core/agent';

export const agent366 = new Agent({
  id: 'agent-366',
  name: 'Agent 366',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
