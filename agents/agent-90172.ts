import { Agent } from '@mastra/core/agent';

export const agent90172 = new Agent({
  id: 'agent-90172',
  name: 'Agent 90172',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
