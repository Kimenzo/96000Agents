import { Agent } from '@mastra/core/agent';

export const agent20013 = new Agent({
  id: 'agent-20013',
  name: 'Agent 20013',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
