import { Agent } from '@mastra/core/agent';

export const agent8886 = new Agent({
  id: 'agent-8886',
  name: 'Agent 8886',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
