import { Agent } from '@mastra/core/agent';

export const agent875 = new Agent({
  id: 'agent-875',
  name: 'Agent 875',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
