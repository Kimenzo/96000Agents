import { Agent } from '@mastra/core/agent';

export const agent93746 = new Agent({
  id: 'agent-93746',
  name: 'Agent 93746',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
