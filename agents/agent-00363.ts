import { Agent } from '@mastra/core/agent';

export const agent363 = new Agent({
  id: 'agent-363',
  name: 'Agent 363',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
