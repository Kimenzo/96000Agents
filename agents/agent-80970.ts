import { Agent } from '@mastra/core/agent';

export const agent80970 = new Agent({
  id: 'agent-80970',
  name: 'Agent 80970',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
