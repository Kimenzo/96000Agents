import { Agent } from '@mastra/core/agent';

export const agent90725 = new Agent({
  id: 'agent-90725',
  name: 'Agent 90725',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
