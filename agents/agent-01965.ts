import { Agent } from '@mastra/core/agent';

export const agent1965 = new Agent({
  id: 'agent-1965',
  name: 'Agent 1965',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
