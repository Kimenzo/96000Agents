import { Agent } from '@mastra/core/agent';

export const agent60840 = new Agent({
  id: 'agent-60840',
  name: 'Agent 60840',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
