import { Agent } from '@mastra/core/agent';

export const agent778 = new Agent({
  id: 'agent-778',
  name: 'Agent 778',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
