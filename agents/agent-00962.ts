import { Agent } from '@mastra/core/agent';

export const agent962 = new Agent({
  id: 'agent-962',
  name: 'Agent 962',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
