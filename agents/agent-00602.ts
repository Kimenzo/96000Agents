import { Agent } from '@mastra/core/agent';

export const agent602 = new Agent({
  id: 'agent-602',
  name: 'Agent 602',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
