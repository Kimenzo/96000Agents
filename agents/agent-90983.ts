import { Agent } from '@mastra/core/agent';

export const agent90983 = new Agent({
  id: 'agent-90983',
  name: 'Agent 90983',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
