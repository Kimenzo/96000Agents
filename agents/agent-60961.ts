import { Agent } from '@mastra/core/agent';

export const agent60961 = new Agent({
  id: 'agent-60961',
  name: 'Agent 60961',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
