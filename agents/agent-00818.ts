import { Agent } from '@mastra/core/agent';

export const agent818 = new Agent({
  id: 'agent-818',
  name: 'Agent 818',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
