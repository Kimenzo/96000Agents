import { Agent } from '@mastra/core/agent';

export const agent60969 = new Agent({
  id: 'agent-60969',
  name: 'Agent 60969',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
