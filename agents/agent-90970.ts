import { Agent } from '@mastra/core/agent';

export const agent90970 = new Agent({
  id: 'agent-90970',
  name: 'Agent 90970',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
