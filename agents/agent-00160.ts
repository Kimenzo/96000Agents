import { Agent } from '@mastra/core/agent';

export const agent160 = new Agent({
  id: 'agent-160',
  name: 'Agent 160',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
