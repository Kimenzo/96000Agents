import { Agent } from '@mastra/core/agent';

export const agent998 = new Agent({
  id: 'agent-998',
  name: 'Agent 998',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
