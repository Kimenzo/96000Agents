import { Agent } from '@mastra/core/agent';

export const agent28587 = new Agent({
  id: 'agent-28587',
  name: 'Agent 28587',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
