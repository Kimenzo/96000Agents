import { Agent } from '@mastra/core/agent';

export const agent80703 = new Agent({
  id: 'agent-80703',
  name: 'Agent 80703',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
