import { Agent } from '@mastra/core/agent';

export const agent60700 = new Agent({
  id: 'agent-60700',
  name: 'Agent 60700',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
