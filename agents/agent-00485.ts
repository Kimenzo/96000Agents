import { Agent } from '@mastra/core/agent';

export const agent485 = new Agent({
  id: 'agent-485',
  name: 'Agent 485',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
