import { Agent } from '@mastra/core/agent';

export const agent861 = new Agent({
  id: 'agent-861',
  name: 'Agent 861',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
