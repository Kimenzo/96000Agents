import { Agent } from '@mastra/core/agent';

export const agent383 = new Agent({
  id: 'agent-383',
  name: 'Agent 383',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
