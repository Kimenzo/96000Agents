import { Agent } from '@mastra/core/agent';

export const agent806 = new Agent({
  id: 'agent-806',
  name: 'Agent 806',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
