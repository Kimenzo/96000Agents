import { Agent } from '@mastra/core/agent';

export const agent563 = new Agent({
  id: 'agent-563',
  name: 'Agent 563',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
