import { Agent } from '@mastra/core/agent';

export const agent60989 = new Agent({
  id: 'agent-60989',
  name: 'Agent 60989',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
