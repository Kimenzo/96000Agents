import { Agent } from '@mastra/core/agent';

export const agent93958 = new Agent({
  id: 'agent-93958',
  name: 'Agent 93958',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
