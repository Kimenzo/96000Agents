import { Agent } from '@mastra/core/agent';

export const agent86727 = new Agent({
  id: 'agent-86727',
  name: 'Agent 86727',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
