import { Agent } from '@mastra/core/agent';

export const agent80588 = new Agent({
  id: 'agent-80588',
  name: 'Agent 80588',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
