import { Agent } from '@mastra/core/agent';

export const agent60503 = new Agent({
  id: 'agent-60503',
  name: 'Agent 60503',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
