import { Agent } from '@mastra/core/agent';

export const agent86600 = new Agent({
  id: 'agent-86600',
  name: 'Agent 86600',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
