import { Agent } from '@mastra/core/agent';

export const agent913 = new Agent({
  id: 'agent-913',
  name: 'Agent 913',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
