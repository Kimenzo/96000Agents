import { Agent } from '@mastra/core/agent';

export const agent90366 = new Agent({
  id: 'agent-90366',
  name: 'Agent 90366',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
