import { Agent } from '@mastra/core/agent';

export const agent90360 = new Agent({
  id: 'agent-90360',
  name: 'Agent 90360',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
