import { Agent } from '@mastra/core/agent';

export const agent937 = new Agent({
  id: 'agent-937',
  name: 'Agent 937',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
