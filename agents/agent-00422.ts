import { Agent } from '@mastra/core/agent';

export const agent422 = new Agent({
  id: 'agent-422',
  name: 'Agent 422',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
