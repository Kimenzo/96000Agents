import { Agent } from '@mastra/core/agent';

export const agent60937 = new Agent({
  id: 'agent-60937',
  name: 'Agent 60937',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
