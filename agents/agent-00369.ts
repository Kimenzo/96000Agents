import { Agent } from '@mastra/core/agent';

export const agent369 = new Agent({
  id: 'agent-369',
  name: 'Agent 369',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
