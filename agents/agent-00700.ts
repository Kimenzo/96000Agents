import { Agent } from '@mastra/core/agent';

export const agent700 = new Agent({
  id: 'agent-700',
  name: 'Agent 700',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
