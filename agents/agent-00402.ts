import { Agent } from '@mastra/core/agent';

export const agent402 = new Agent({
  id: 'agent-402',
  name: 'Agent 402',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
