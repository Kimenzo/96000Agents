import { Agent } from '@mastra/core/agent';

export const agent90737 = new Agent({
  id: 'agent-90737',
  name: 'Agent 90737',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
