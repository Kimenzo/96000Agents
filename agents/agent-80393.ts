import { Agent } from '@mastra/core/agent';

export const agent80393 = new Agent({
  id: 'agent-80393',
  name: 'Agent 80393',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
