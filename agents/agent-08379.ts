import { Agent } from '@mastra/core/agent';

export const agent8379 = new Agent({
  id: 'agent-8379',
  name: 'Agent 8379',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
