import { Agent } from '@mastra/core/agent';

export const agent90706 = new Agent({
  id: 'agent-90706',
  name: 'Agent 90706',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
