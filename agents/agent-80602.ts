import { Agent } from '@mastra/core/agent';

export const agent80602 = new Agent({
  id: 'agent-80602',
  name: 'Agent 80602',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
