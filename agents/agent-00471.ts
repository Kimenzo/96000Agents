import { Agent } from '@mastra/core/agent';

export const agent471 = new Agent({
  id: 'agent-471',
  name: 'Agent 471',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
