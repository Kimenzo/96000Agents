import { Agent } from '@mastra/core/agent';

export const agent206 = new Agent({
  id: 'agent-206',
  name: 'Agent 206',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
