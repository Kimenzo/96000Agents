import { Agent } from '@mastra/core/agent';

export const agent80321 = new Agent({
  id: 'agent-80321',
  name: 'Agent 80321',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
