import { Agent } from '@mastra/core/agent';

export const agent262 = new Agent({
  id: 'agent-262',
  name: 'Agent 262',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
