import { Agent } from '@mastra/core/agent';

export const agent90003 = new Agent({
  id: 'agent-90003',
  name: 'Agent 90003',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
