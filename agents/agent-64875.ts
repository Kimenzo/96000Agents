import { Agent } from '@mastra/core/agent';

export const agent64875 = new Agent({
  id: 'agent-64875',
  name: 'Agent 64875',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
