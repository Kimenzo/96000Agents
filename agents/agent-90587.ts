import { Agent } from '@mastra/core/agent';

export const agent90587 = new Agent({
  id: 'agent-90587',
  name: 'Agent 90587',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
