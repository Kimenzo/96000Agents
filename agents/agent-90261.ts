import { Agent } from '@mastra/core/agent';

export const agent90261 = new Agent({
  id: 'agent-90261',
  name: 'Agent 90261',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
