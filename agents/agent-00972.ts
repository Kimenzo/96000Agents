import { Agent } from '@mastra/core/agent';

export const agent972 = new Agent({
  id: 'agent-972',
  name: 'Agent 972',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
