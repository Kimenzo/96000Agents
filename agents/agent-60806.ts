import { Agent } from '@mastra/core/agent';

export const agent60806 = new Agent({
  id: 'agent-60806',
  name: 'Agent 60806',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
