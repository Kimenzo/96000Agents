import { Agent } from '@mastra/core/agent';

export const agent720 = new Agent({
  id: 'agent-720',
  name: 'Agent 720',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
