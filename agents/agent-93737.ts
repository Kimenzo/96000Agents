import { Agent } from '@mastra/core/agent';

export const agent93737 = new Agent({
  id: 'agent-93737',
  name: 'Agent 93737',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
