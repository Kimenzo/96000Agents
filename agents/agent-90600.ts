import { Agent } from '@mastra/core/agent';

export const agent90600 = new Agent({
  id: 'agent-90600',
  name: 'Agent 90600',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
