import { Agent } from '@mastra/core/agent';

export const agent920 = new Agent({
  id: 'agent-920',
  name: 'Agent 920',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
