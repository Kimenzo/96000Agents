import { Agent } from '@mastra/core/agent';

export const agent800 = new Agent({
  id: 'agent-800',
  name: 'Agent 800',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
