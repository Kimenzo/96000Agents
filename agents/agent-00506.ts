import { Agent } from '@mastra/core/agent';

export const agent506 = new Agent({
  id: 'agent-506',
  name: 'Agent 506',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
