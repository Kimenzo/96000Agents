import { Agent } from '@mastra/core/agent';

export const agent90974 = new Agent({
  id: 'agent-90974',
  name: 'Agent 90974',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
