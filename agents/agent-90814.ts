import { Agent } from '@mastra/core/agent';

export const agent90814 = new Agent({
  id: 'agent-90814',
  name: 'Agent 90814',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
