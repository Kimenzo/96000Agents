import { Agent } from '@mastra/core/agent';

export const agent60875 = new Agent({
  id: 'agent-60875',
  name: 'Agent 60875',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
