import { Agent } from '@mastra/core/agent';

export const agent36962 = new Agent({
  id: 'agent-36962',
  name: 'Agent 36962',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
