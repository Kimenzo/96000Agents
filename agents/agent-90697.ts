import { Agent } from '@mastra/core/agent';

export const agent90697 = new Agent({
  id: 'agent-90697',
  name: 'Agent 90697',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
