import { Agent } from '@mastra/core/agent';

export const agent204 = new Agent({
  id: 'agent-204',
  name: 'Agent 204',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
