import { Agent } from '@mastra/core/agent';

export const agent203 = new Agent({
  id: 'agent-203',
  name: 'Agent 203',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
