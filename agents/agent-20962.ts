import { Agent } from '@mastra/core/agent';

export const agent20962 = new Agent({
  id: 'agent-20962',
  name: 'Agent 20962',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
