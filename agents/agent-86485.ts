import { Agent } from '@mastra/core/agent';

export const agent86485 = new Agent({
  id: 'agent-86485',
  name: 'Agent 86485',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
