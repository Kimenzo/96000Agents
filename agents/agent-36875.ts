import { Agent } from '@mastra/core/agent';

export const agent36875 = new Agent({
  id: 'agent-36875',
  name: 'Agent 36875',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
