import { Agent } from '@mastra/core/agent';

export const agent804 = new Agent({
  id: 'agent-804',
  name: 'Agent 804',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
