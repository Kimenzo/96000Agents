import { Agent } from '@mastra/core/agent';

export const agent93964 = new Agent({
  id: 'agent-93964',
  name: 'Agent 93964',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
