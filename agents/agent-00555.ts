import { Agent } from '@mastra/core/agent';

export const agent555 = new Agent({
  id: 'agent-555',
  name: 'Agent 555',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
