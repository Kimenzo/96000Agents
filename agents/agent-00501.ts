import { Agent } from '@mastra/core/agent';

export const agent501 = new Agent({
  id: 'agent-501',
  name: 'Agent 501',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
