import { Agent } from '@mastra/core/agent';

export const agent566 = new Agent({
  id: 'agent-566',
  name: 'Agent 566',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
