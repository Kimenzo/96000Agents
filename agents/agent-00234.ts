import { Agent } from '@mastra/core/agent';

export const agent234 = new Agent({
  id: 'agent-234',
  name: 'Agent 234',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
