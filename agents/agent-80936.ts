import { Agent } from '@mastra/core/agent';

export const agent80936 = new Agent({
  id: 'agent-80936',
  name: 'Agent 80936',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
