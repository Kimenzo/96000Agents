import { Agent } from '@mastra/core/agent';

export const agent781 = new Agent({
  id: 'agent-781',
  name: 'Agent 781',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
