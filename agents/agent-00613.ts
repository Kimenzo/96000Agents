import { Agent } from '@mastra/core/agent';

export const agent613 = new Agent({
  id: 'agent-613',
  name: 'Agent 613',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
