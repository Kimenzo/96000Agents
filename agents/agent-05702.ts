import { Agent } from '@mastra/core/agent';

export const agent5702 = new Agent({
  id: 'agent-5702',
  name: 'Agent 5702',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
