import { Agent } from '@mastra/core/agent';

export const agent777 = new Agent({
  id: 'agent-777',
  name: 'Agent 777',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
