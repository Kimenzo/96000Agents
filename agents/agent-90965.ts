import { Agent } from '@mastra/core/agent';

export const agent90965 = new Agent({
  id: 'agent-90965',
  name: 'Agent 90965',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
