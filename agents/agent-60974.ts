import { Agent } from '@mastra/core/agent';

export const agent60974 = new Agent({
  id: 'agent-60974',
  name: 'Agent 60974',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
