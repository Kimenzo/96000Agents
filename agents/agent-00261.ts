import { Agent } from '@mastra/core/agent';

export const agent261 = new Agent({
  id: 'agent-261',
  name: 'Agent 261',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
