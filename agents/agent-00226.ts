import { Agent } from '@mastra/core/agent';

export const agent226 = new Agent({
  id: 'agent-226',
  name: 'Agent 226',
  instructions: 'Awaiting skill assignment.',
  model: 'openai/gpt-4o-mini',
});
